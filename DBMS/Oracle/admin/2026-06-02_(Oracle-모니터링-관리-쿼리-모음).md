# Oracle 모니터링·관리 쿼리 모음
- **카테고리**: #DBMS #Oracle
- **태그**: #Oracle #admin #모니터링 #쿼리모음
- **작성일**: 2026-06-02
- **참조 원본**: [[2026-06-02-ASH(Active Session History)조회쿼리]], [[2026-06-02-v$sql수행통계정보조회쿼리]], [[2026-06-02-lock 모니터링 쿼리]], [[2026-06-02-dictionary table검색쿼리]]

## 1. 핵심 요약
- Oracle DBA가 현장에서 자주 사용하는 모니터링·관리 쿼리 모음. ASH, v$sql, Lock, Dictionary 조회 포함.

## 2. 상세 설명

### ASH (Active Session History) 조회

현재 Active 세션을 1초 단위 샘플링. 접속이 끊긴 세션도 포함. AWR에 저장.

```sql
SELECT
  sample_id, sample_time              -- (1) 샘플링 시간
, session_id, session_serial#, user_id, xid  -- (2) 세션·트랜잭션 정보
, sql_id, sql_child_number, sql_plan_hash_value  -- (3) 수행 SQL
, session_state                       -- (4) ON CPU / WAITING
, qc_instance_id, qc_session_id       -- (5) 병렬 QC 정보
, blocking_session, blocking_session_serial#, blocking_session_status  -- (6) Blocking 세션
, event, event#, seq#, wait_class, wait_time, time_waited  -- (7) 대기 이벤트
, p1text, p1, p2text, p2, p3text, p3  -- (8) 대기 파라미터
, current_obj#, current_file#, current_block#  -- (9) 참조 오브젝트
, program, module, action, client_id  -- (10) 애플리케이션 정보
FROM V$ACTIVE_SESSION_HISTORY
WHERE rownum <= 10;
```

---

### v$sql 수행 통계 조회 (스키마별)

지난 7일 내 수행된 SQL의 스키마별 통계. 느린 SQL("BAD SQL") 카운트 포함.

```sql
SELECT parsing_schema_name
, count(*)                                        sql_cnt
, count(distinct substr(sql_text, 1, 100))        sql_cnt2
, sum(executions)                                 executions
, round(avg(buffer_gets/executions))              buffer_gets
, round(avg(disk_reads/executions))               disk_reads
, round(avg(rows_processed/executions))           rows_processed
, round(avg(elapsed_time/executions/1000000),2)   "ELAPSED_TIME(AVG)"
, count(case when elapsed_time/executions/1000000 >= 10 then 1 end)  "BAD SQL"
, round(max(elapsed_time/executions/1000000),2)   "ELAPSED_TIME(MAX)"
FROM v$sql
WHERE last_active_time >= sysdate - 7
AND executions > 0
GROUP BY parsing_schema_name;
```

---

### Lock 모니터링

**1. 전체 Lock 대기 상황**

```sql
SELECT l.session_id SID
     ,(CASE WHEN lock_type = 'Transaction' THEN 'TX'
            WHEN lock_type = 'DML'         THEN 'TM' END) TYPE
     , mode_held
     , mode_requested mode_reqd
     ,(CASE WHEN lock_type = 'Transaction' THEN to_char(trunc(lock_id1/power(2,16)))
            WHEN lock_type = 'DML'         THEN (SELECT object_name FROM dba_objects WHERE object_id = l.lock_id1) END) "USN/Table"
     ,(CASE WHEN lock_type = 'Transaction' THEN bitand(lock_id1, to_number('ffff','xxxx')) + 0 END) "SLOT"
     ,(CASE WHEN lock_type = 'Transaction' THEN to_number(lock_id2) END) "SQN"
     ,(CASE WHEN blocking_others = 'Blocking' THEN ' <<<<<' END) Blocking
FROM   dba_lock l
WHERE  lock_type IN ('Transaction', 'DML')
ORDER BY session_id, lock_type, lock_id1, lock_id2;
```

**2. 현재 유저 Lock 상태**

```sql
SELECT username, v$lock.sid, id1, id2, lmode, request, block, v$lock.type
FROM v$lock, v$session
WHERE v$lock.sid = v$session.sid
AND   v$session.username = USER;
```

---

### Dictionary Table 검색

```sql
-- 특정 패턴으로 Dictionary View 검색
SELECT * FROM dict WHERE table_name LIKE 'DBA_HIST_SQL%';

-- 결과 예시
-- DBA_HIST_SQLBIND, DBA_HIST_SQLSTAT, DBA_HIST_SQLTEXT 등
```

## 3. 연관 개념 (지식 연결)
_(관련 문서가 생성되면 여기에 백링크를 추가합니다.)_
