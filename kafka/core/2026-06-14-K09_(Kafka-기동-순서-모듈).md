# Kafka 기동 순서 (모듈별)

- **카테고리**: #kafka #install
- **태그**: #kafka #start #기동 #schema-registry #connect
- **작성일**: 2026-06-14
- **참조 원본**: [[2026-06-13-kafka start - BigDataTeam]]

## 1. 핵심 요약

Confluent Kafka는 **기본 모듈(zookeeper → kafka → schema-registry)** 을 모두 기동한 뒤 **추가 모듈(connect)** 을 실행합니다.

---

## 2. 기본 모듈 (순서대로)

```bash
# 1) zookeeper
bin/zookeeper-server-start etc/kafka/zookeeper.properties
# 2) kafka broker
bin/kafka-server-start etc/kafka/server.properties
# 3) schema registry
bin/schema-registry-start etc/schema-registry/schema-registry.properties
```

## 3. 추가 모듈

```bash
# connect (분산 모드)
bin/connect-distributed etc/schema-registry/connect-avro-distributed.properties
```

> `-daemon` 옵션을 붙이면 백그라운드 기동. 중지는 대응 `*-stop` 스크립트 사용.

## 4. 연관 개념

- [[2026-06-14-K06_(Kafka-설치-Apache-Confluent-Debezium-CDC)]]
- [[2026-06-14-K07_(Kafka-Broker-server.properties-설정)]]
- [[2026-06-14-K08_(Kafka-Zookeeper-properties-설정)]]
- [[2026-06-14-K10_(Kafka-Topic-Producer-Consumer-명령어)]]
